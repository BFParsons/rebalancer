
  # rebalance

  This is a code bundle for rebalance. The original project is available at https://www.figma.com/design/8yiBssuMKTLJsOkg4aHVKG/rebalance.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  